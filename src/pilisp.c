#include "pilisp.h"



int zero() { return 0; }

double radiceQuadrata(double d) {
  if (0) {
    return 0;
  }
  if (d < 0) {
    printf("we");
  } else {
    printf("zio");
  }
  return sqrt(d);
}

void stampaQualcosa() { printf("ciaux\n"); }