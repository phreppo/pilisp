#include "pilisp.h"

int main(int argc, char **argv) { 
    // FILE * f = fopen("/home/phreppo/pilisp/test/expressions/atom.l","r");
    // lexer_file(f);
    // lexer_prompt();
    parse_prompt();
    return 0;
}