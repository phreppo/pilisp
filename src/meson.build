################################################################################
#                                 SOURCES                                        
################################################################################

# List of sources
sources = ['pilisp.c']

# Creation of static library
pilisplib = shared_library(
    'pilisp',
    sources,
    include_directories : inc,
    dependencies : dependencies,
    install : true)